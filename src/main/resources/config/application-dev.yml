spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/core?useUnicode=true&charset=utf8mb4&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password:
    hikari:
      minimum-idle: 10
      maximum-pool-size: 20

kakao:
  token:

# AWS Setting
cloud:
  aws:
    stack:
      auto: false
    region:
      static: ap-northeast-2
    credentials:
      access-key:
      secret-key:
    s3:
      bucket: core-image-bucket
      url: https://core-image-bucket.s3.ap-northeast-2.amazonaws.com
